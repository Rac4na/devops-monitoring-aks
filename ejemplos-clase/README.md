# Ejemplos de clase

Manifiestos y valores usados durante las clases 1 y 2 (referencia). No forman parte del despliegue del proyecto final.

- `pod.yaml`: pod con nginx + busybox que escribe logs.
- `nginx-deployment.yaml`: Deployment de nginx con 5 réplicas.
- `values.yaml`: reglas de alertas y Alertmanager por correo (la contraseña SMTP se reemplazó por un placeholder).
